<%= render 'spree/admin/shared/product_sub_menu' %>
<%= render partial: 'spree/admin/shared/product_tabs', locals: { current: Spree.t(:volume_pricing) } %>
<%= render "spree/shared/error_messages", :target => @variant %>

<%= form_for(@variant, :url => admin_product_variant_path(@product, @variant), :html => { :method => :put }) do |f| %>
  <h3><%= Spree.t(:volume_price_models) %></h3>
  <table class="index">
    <thead>
      <tr>
        <th><%= Spree.t(:name) %></th>
        <th class="actions"></th>
      </tr>
    </thead>
    <tbody id="volume_price_models">
      <%= f.fields_for :volume_price_models do |vp_form| %>
        <%= render 'volume_price_model_fields', :f => vp_form %>
      <% end %>
    </tbody>
  </table>
  <%= link_to_add_fields Spree.t(:add_volume_price_models), "tbody#volume_price_models", {:class => 'new_volume_price_row'} %>
  <br/><br/>

  <%= render 'spree/admin/shared/edit_resource_links' %>
<% end %>
